'use client';
export default function FormModal({open,title,onClose,children}:{open:boolean,title:string,onClose:()=>void,children:React.ReactNode}){if(!open)return null;return <div style={{position:'fixed',inset:0,background:'rgba(0,0,0,.55)',zIndex:50,display:'grid',placeItems:'center',padding:16}} onMouseDown={onClose}><div className="card" style={{width:'min(720px,100%)',maxHeight:'90vh',overflow:'auto'}} onMouseDown={e=>e.stopPropagation()}><div className="title"><h1 style={{fontSize:18}}>{title}</h1><button className="btn" style={{background:'#333'}} onClick={onClose}>بستن</button></div>{children}</div></div>}
