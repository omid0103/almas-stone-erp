'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';
const money=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));
export default function Invoices(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('sales_invoices').select('id,invoice_no,invoice_date,jalali_date,status,total,legacy_balance,customer:parties(display_name)').order('invoice_date',{ascending:false}).limit(200).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>فروش و فاکتور</h1><p>سوابق فاکتورهای فروش و مانده مشتری</p></div><span className="badge">{rows.length} رکورد</span></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>شماره</th><th>تاریخ</th><th>مشتری</th><th>مبلغ کل</th><th>مانده</th><th>وضعیت</th></tr></thead><tbody>{rows.map((r:any)=><tr key={r.id}><td>{r.invoice_no}</td><td>{r.jalali_date||r.invoice_date||'-'}</td><td>{r.customer?.display_name||'-'}</td><td>{money(r.total)}</td><td>{money(r.legacy_balance)}</td><td>{r.status}</td></tr>)}</tbody></table></div></>}
