'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';export default function Inventory(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('v_product_stock').select('*').limit(300).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>انبار</h1><p>موجودی محصول نهایی و گردش انبار</p></div></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>محصول</th><th>موجودی</th><th>حداقل موجودی</th><th>وضعیت</th></tr></thead><tbody>{rows.length?rows.map((r:any,i)=><tr key={r.product_id||i}><td>{r.product_name||r.name||'-'}</td><td>{r.stock??r.quantity??0}</td><td>{r.min_stock??0}</td><td>{Number(r.stock??0)<=Number(r.min_stock??0)?'نیاز به تأمین':'عادی'}</td></tr>):<tr><td colSpan={4}>هنوز گردش انبار ثبت نشده است.</td></tr>}</tbody></table></div></>}
