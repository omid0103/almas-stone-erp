'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';import {currentJalaliMonth,currentJalaliYear,todayJalali,toJalali} from '@/lib/jalali';import FormModal from '@/components/FormModal';
const money=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));const status:any={present:'حاضر',absent:'غایب',leave:'مرخصی',half_day:'نیم‌روز'};
export default function Employees(){const[rows,setRows]=useState<any[]>([]),[payroll,setPayroll]=useState<any[]>([]),[attendance,setAttendance]=useState<any[]>([]),[openEmp,setOpenEmp]=useState(false),[openPay,setOpenPay]=useState(false),[openAtt,setOpenAtt]=useState(false),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);const[emp,setEmp]=useState<any>({full_name:'',phone:'',job_title:'',base_salary:'',bank_account:'',active:true});const[pay,setPay]=useState<any>({employee_id:'',year:currentJalaliYear(),month:currentJalaliMonth(),workdays:'',overtime_amount:'',gross_wage:'',advances:'',deductions:'',paid:'',notes:''});const[att,setAtt]=useState<any>({employee_id:'',jalali_date:todayJalali(),status:'present',overtime_minutes:0,notes:''});async function load(){const s=supabase();const[{data:e},{data:p},{data:a}]=await Promise.all([s.from('employees').select('id,full_name,phone,job_title,base_salary,bank_account,active').order('full_name'),s.from('payroll_periods').select('id,year,month,workdays,overtime_amount,gross_wage,advances,deductions,net_payable,paid,notes,employee:employees(full_name)').order('year',{ascending:false}).order('month',{ascending:false}).limit(200),s.from('attendance').select('id,work_date,jalali_date,status,overtime_minutes,notes,employee:employees(full_name)').order('work_date',{ascending:false}).limit(200)]);setRows(e||[]);setPayroll(p||[]);setAttendance(a||[])}useEffect(()=>{load()},[]);async function saveEmp(){if(!emp.full_name.trim()){setMsg('نام کارمند الزامی است.');return}setBusy(true);const{error}=await supabase().from('employees').insert({full_name:emp.full_name.trim(),phone:emp.phone||null,job_title:emp.job_title||null,base_salary:emp.base_salary?Number(emp.base_salary):null,bank_account:emp.bank_account||null,active:!!emp.active});setBusy(false);if(error){setMsg(error.message);return}setOpenEmp(false);setEmp({full_name:'',phone:'',job_title:'',base_salary:'',bank_account:'',active:true});setMsg('');load()}async function savePay(){if(!pay.employee_id){setMsg('کارمند را انتخاب کنید.');return}const gross=Number(pay.gross_wage||0),overtime=Number(pay.overtime_amount||0),advances=Number(pay.advances||0),deductions=Number(pay.deductions||0),net=Math.max(0,gross+overtime-advances-deductions);setBusy(true);const{error}=await supabase().from('payroll_periods').upsert({employee_id:pay.employee_id,year:Number(pay.year),month:Number(pay.month),workdays:Number(pay.workdays||0),overtime_amount:overtime,gross_wage:gross,advances,deductions,net_payable:net,paid:Number(pay.paid||0),notes:pay.notes||null},{onConflict:'employee_id,year,month'});setBusy(false);if(error){setMsg(error.message);return}setOpenPay(false);setPay({employee_id:'',year:currentJalaliYear(),month:currentJalaliMonth(),workdays:'',overtime_amount:'',gross_wage:'',advances:'',deductions:'',paid:'',notes:''});setMsg('');load()}async function saveAtt(){if(!att.employee_id){setMsg('کارمند را انتخاب کنید.');return}setBusy(true);const{error}=await supabase().from('attendance').upsert({employee_id:att.employee_id,work_date:new Date().toISOString().slice(0,10),jalali_date:att.jalali_date||todayJalali(),status:att.status,overtime_minutes:Number(att.overtime_minutes||0),notes:att.notes||null},{onConflict:'employee_id,work_date'});setBusy(false);if(error){setMsg(error.message);return}setOpenAtt(false);setAtt({employee_id:'',jalali_date:todayJalali(),status:'present',overtime_minutes:0,notes:''});setMsg('');load()}return <><div className="title"><div><h1>کارکنان، حضور و حقوق</h1><p>مدیریت نیروی کار، حضور روزانه و پرداخت حقوق</p></div><div style={{display:'flex',gap:8,flexWrap:'wrap'}}><span className="badge">{rows.length} نفر</span><button className="btn" onClick={()=>{setMsg('');setOpenEmp(true)}}>+ کارمند</button><button className="btn" style={{background:'#555'}} onClick={()=>{setMsg('');setOpenAtt(true)}}>+ حضور و غیاب</button><button className="btn" style={{background:'#333'}} onClick={()=>{setMsg('');setOpenPay(true)}}>+ حقوق</button></div></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>نام</th><th>سمت</th><th>موبایل</th><th>حقوق پایه</th><th>وضعیت</th></tr></thead><tbody>{rows.length?rows.map(r=><tr key={r.id}><td>{r.full_name}</td><td>{r.job_title||'-'}</td><td>{r.phone||'-'}</td><td>{money(r.base_salary)}</td><td>{r.active?'فعال':'غیرفعال'}</td></tr>):<tr><td colSpan={5} style={{textAlign:'center',padding:24}}>هنوز کارمندی ثبت نشده است.</td></tr>}</tbody></table></div><div className="section"><h2 style={{fontSize:18}}>آخرین حضور و غیاب</h2><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>تاریخ</th><th>کارمند</th><th>وضعیت</th><th>اضافه‌کار</th><th>یادداشت</th></tr></thead><tbody>{attendance.length?attendance.map((a:any)=><tr key={a.id}><td>{a.jalali_date||toJalali(a.work_date)}</td><td>{a.employee?.full_name||'-'}</td><td>{status[a.status]||a.status}</td><td>{a.overtime_minutes||0} دقیقه</td><td>{a.notes||'-'}</td></tr>):<tr><td colSpan={5}>رکوردی ثبت نشده است.</td></tr>}</tbody></table></div></div><div className="section"><h2 style={{fontSize:18}}>سوابق حقوق</h2><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>کارمند</th><th>سال/ماه شمسی</th><th>روزکار</th><th>حقوق ناخالص</th><th>اضافه‌کار</th><th>خالص پرداختنی</th><th>پرداخت‌شده</th></tr></thead><tbody>{payroll.length?payroll.map((p:any)=><tr key={p.id}><td>{p.employee?.full_name||'-'}</td><td>{p.year}/{String(p.month).padStart(2,'0')}</td><td>{p.workdays}</td><td>{money(p.gross_wage)}</td><td>{money(p.overtime_amount)}</td><td>{money(p.net_payable)}</td><td>{money(p.paid)}</td></tr>):<tr><td colSpan={7}>هنوز حقوقی ثبت نشده است.</td></tr>}</tbody></table></div></div><FormModal open={openEmp} title="ثبت کارمند جدید" onClose={()=>setOpenEmp(false)}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}><div className="field"><label>نام و نام خانوادگی *</label><input className="input" value={emp.full_name} onChange={e=>setEmp({...emp,full_name:e.target.value})}/></div><div className="field"><label>سمت</label><input className="input" value={emp.job_title} onChange={e=>setEmp({...emp,job_title:e.target.value})}/></div><div className="field"><label>موبایل</label><input className="input" value={emp.phone} onChange={e=>setEmp({...emp,phone:e.target.value})}/></div><div className="field"><label>حقوق پایه</label><input className="input" type="number" value={emp.base_salary} onChange={e=>setEmp({...emp,base_salary:e.target.value})}/></div><div className="field"><label>حساب بانکی</label><input className="input" value={emp.bank_account} onChange={e=>setEmp({...emp,bank_account:e.target.value})}/></div></div>{msg&&<div className="msg">{msg}</div>}<button className="btn" style={{width:'100%',marginTop:12}} disabled={busy} onClick={saveEmp}>ثبت کارمند</button></FormModal><FormModal open={openAtt} title="ثبت حضور و غیاب" onClose={()=>setOpenAtt(false)}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}><div className="field"><label>کارمند *</label><select className="input" value={att.employee_id} onChange={e=>setAtt({...att,employee_id:e.target.value})}><option value="">انتخاب</option>{rows.filter(r=>r.active).map(r=><option key={r.id} value={r.id}>{r.full_name}</option>)}</select></div><div className="field"><label>تاریخ شمسی</label><input className="input" value={att.jalali_date} onChange={e=>setAtt({...att,jalali_date:e.target.value})}/></div><div className="field"><label>وضعیت</label><select className="input" value={att.status} onChange={e=>setAtt({...att,status:e.target.value})}>{Object.entries(status).map(([k,v])=><option key={k} value={k}>{v as string}</option>)}</select></div><div className="field"><label>اضافه‌کار (دقیقه)</label><input className="input" type="number" min="0" value={att.overtime_minutes} onChange={e=>setAtt({...att,overtime_minutes:e.target.value})}/></div></div><div className="field"><label>یادداشت</label><textarea className="input" rows={2} value={att.notes} onChange={e=>setAtt({...att,notes:e.target.value})}/></div>{msg&&<div className="msg">{msg}</div>}<button className="btn" style={{width:'100%',marginTop:12}} disabled={busy} onClick={saveAtt}>ثبت حضور</button></FormModal><FormModal open={openPay} title="ثبت حقوق" onClose={()=>setOpenPay(false)}><div style={{display:'grid',gridTemplateColumns:'1fr 1fr',gap:10}}><div className="field"><label>کارمند *</label><select className="input" value={pay.employee_id} onChange={e=>setPay({...pay,employee_id:e.target.value})}><option value="">انتخاب</option>{rows.filter(r=>r.active).map(r=><option key={r.id} value={r.id}>{r.full_name}</option>)}</select></div><div className="field"><label>سال شمسی</label><input className="input" type="number" value={pay.year} onChange={e=>setPay({...pay,year:e.target.value})}/></div><div className="field"><label>ماه شمسی</label><input className="input" type="number" min="1" max="12" value={pay.month} onChange={e=>setPay({...pay,month:e.target.value})}/></div><div className="field"><label>روزکار</label><input className="input" type="number" value={pay.workdays} onChange={e=>setPay({...pay,workdays:e.target.value})}/></div><div className="field"><label>حقوق ناخالص</label><input className="input" type="number" value={pay.gross_wage} onChange={e=>setPay({...pay,gross_wage:e.target.value})}/></div><div className="field"><label>اضافه‌کار</label><input className="input" type="number" value={pay.overtime_amount} onChange={e=>setPay({...pay,overtime_amount:e.target.value})}/></div><div className="field"><label>علی‌الحساب</label><input className="input" type="number" value={pay.advances} onChange={e=>setPay({...pay,advances:e.target.value})}/></div><div className="field"><label>کسورات</label><input className="input" type="number" value={pay.deductions} onChange={e=>setPay({...pay,deductions:e.target.value})}/></div><div className="field"><label>پرداخت‌شده</label><input className="input" type="number" value={pay.paid} onChange={e=>setPay({...pay,paid:e.target.value})}/></div></div><div className="field"><label>یادداشت</label><textarea className="input" rows={2} value={pay.notes} onChange={e=>setPay({...pay,notes:e.target.value})}/></div>{msg&&<div className="msg">{msg}</div>}<button className="btn" style={{width:'100%',marginTop:12}} disabled={busy} onClick={savePay}>ثبت / بروزرسانی حقوق</button></FormModal></>}
