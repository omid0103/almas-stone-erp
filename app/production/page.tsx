'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';export default function Production(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('production_batches').select('id,batch_no,quantity,waste_quantity,jalali_date,status,product:products(name)').order('production_date',{ascending:false}).limit(200).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>تولید</h1><p>ثبت تولید روزانه، ضایعات و وضعیت بچ تولید</p></div></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>بچ</th><th>تاریخ</th><th>محصول</th><th>تعداد</th><th>ضایعات</th><th>وضعیت</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.id}><td>{r.batch_no}</td><td>{r.jalali_date||'-'}</td><td>{r.product?.name||'-'}</td><td>{r.quantity}</td><td>{r.waste_quantity}</td><td>{r.status}</td></tr>):<tr><td colSpan={6}>هنوز تولیدی ثبت نشده است.</td></tr>}</tbody></table></div></>}
