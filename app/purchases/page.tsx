'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';const money=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));export default function Purchases(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('purchase_invoices').select('id,invoice_no,invoice_date,jalali_date,total,status,supplier:parties(display_name)').order('invoice_date',{ascending:false}).limit(200).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>خرید و تأمین‌کنندگان</h1><p>فاکتور خرید، بدهی و حساب تأمین‌کننده</p></div></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>شماره</th><th>تاریخ</th><th>تأمین‌کننده</th><th>مبلغ</th><th>وضعیت</th></tr></thead><tbody>{rows.length?rows.map((r:any)=><tr key={r.id}><td>{r.invoice_no||'-'}</td><td>{r.jalali_date||r.invoice_date||'-'}</td><td>{r.supplier?.display_name||'-'}</td><td>{money(r.total)}</td><td>{r.status||'-'}</td></tr>):<tr><td colSpan={5}>هنوز خریدی ثبت نشده است.</td></tr>}</tbody></table></div></>}
