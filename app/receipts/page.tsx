'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';const money=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));export default function Receipts(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('financial_transactions').select('id,direction,method,amount,jalali_date,description').order('created_at',{ascending:false}).limit(300).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>دریافت و پرداخت</h1><p>تراکنش‌های نقد، کارت، بانک و چک</p></div><span className="badge">{rows.length} رکورد</span></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>تاریخ</th><th>نوع</th><th>شرح</th><th>روش</th><th>مبلغ</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.jalali_date||'-'}</td><td>{r.direction==='receipt'?'دریافت':'پرداخت'}</td><td>{r.description||'-'}</td><td>{r.method}</td><td>{money(r.amount)}</td></tr>)}</tbody></table></div></>}
