'use client';
import {useEffect,useState} from 'react';
import {supabase} from '@/lib/supabase';
export default function Settings(){const[f,setF]=useState<any>({company_name:'صنایع سنگ الماس',english_name:'ALMAS STONE',currency:'toman',phone:'',address:'',invoice_footer:''}),[msg,setMsg]=useState(''),[busy,setBusy]=useState(false);useEffect(()=>{supabase().from('company_settings').select('*').eq('id',true).maybeSingle().then(({data})=>data&&setF({...f,...data}))},[]);async function save(){setBusy(true);setMsg('');const{error}=await supabase().from('company_settings').upsert({id:true,company_name:f.company_name||'صنایع سنگ الماس',english_name:f.english_name||'ALMAS STONE',currency:f.currency||'toman',phone:f.phone||null,address:f.address||null,invoice_footer:f.invoice_footer||null,updated_at:new Date().toISOString()});setBusy(false);setMsg(error?error.message:'تنظیمات با موفقیت ذخیره شد.')}return <><div className="title"><div><h1>تنظیمات</h1><p>مشخصات شرکت، واحد پول و اطلاعات فاکتور</p></div></div><div className="card"><div className="form"><label>نام مجموعه<input className="input" value={f.company_name} onChange={e=>setF({...f,company_name:e.target.value})}/></label><label>نام انگلیسی<input className="input" value={f.english_name} onChange={e=>setF({...f,english_name:e.target.value})}/></label><label>واحد پول<select className="input" value={f.currency} onChange={e=>setF({...f,currency:e.target.value})}><option value="toman">تومان</option><option value="rial">ریال</option></select></label><label>تلفن<input className="input" value={f.phone||''} onChange={e=>setF({...f,phone:e.target.value})}/></label><label>آدرس<textarea className="input" rows={2} value={f.address||''} onChange={e=>setF({...f,address:e.target.value})}/></label><label>متن پایین فاکتور<textarea className="input" rows={3} value={f.invoice_footer||''} onChange={e=>setF({...f,invoice_footer:e.target.value})} placeholder="با تشکر از خرید شما..."/></label>{msg&&<div className="msg">{msg}</div>}<button className="btn" disabled={busy} onClick={save}>{busy?'در حال ذخیره...':'ذخیره تنظیمات'}</button></div></div></>}
