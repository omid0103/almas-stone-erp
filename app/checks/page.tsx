'use client';
import {useEffect,useState} from 'react';import {supabase} from '@/lib/supabase';
const money=(n:any)=>new Intl.NumberFormat('fa-IR').format(Number(n||0));
export default function Checks(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase().from('checks').select('id,direction,owner_name,transferor_recipient,bank_branch,check_serial,amount,due_jalali,due_date,status').order('due_date',{ascending:true}).then(({data})=>setRows(data||[]))},[]);return <><div className="title"><div><h1>چک‌ها</h1><p>چک‌های دریافتی و پرداختی</p></div><span className="badge">{rows.length} فقره</span></div><div style={{overflow:'auto'}}><table className="table"><thead><tr><th>نوع</th><th>صاحب/طرف</th><th>بانک</th><th>شماره</th><th>مبلغ</th><th>سررسید</th><th>وضعیت</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.direction==='receivable'?'دریافتی':'پرداختی'}</td><td>{r.owner_name||r.transferor_recipient||'-'}</td><td>{r.bank_branch||'-'}</td><td>{r.check_serial||'-'}</td><td>{money(r.amount)}</td><td>{r.due_jalali||r.due_date||'-'}</td><td>{r.status}</td></tr>)}</tbody></table></div></>}
